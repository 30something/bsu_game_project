#pragma once

#include <QFont>

namespace fonts {

const QFont kGameNameLabelFont("Arial", 40, 0, true);
const QFont kDefaultInfoFont("Arial", 20, 0, false);
const QFont kStartInfoFont("Arial", 30, 0, true);
const QFont kDefaultLabelFont("Arial", 10, 0, false);
const QFont kDefaultButtonFont("Arial", 10, 0, false);

}
